{
  "name": "loom",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Narrative-driven generator of long-term conversational memory datasets. LoCoMo-compatible.",
  "scripts": {
    "test": "vitest run",
    "generate": "tsx src/cli.ts"
  },
  "dependencies": {
    "yaml": "^2.8.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
